
Install wine:

```bash
apt-get install wine
```

Install winetricks:

```bash
wget winetricks.googlecode.com/git/src/winetricks
chmod 755 winetricks
```

Install Steam:

```bash
./winetricks steam
```
